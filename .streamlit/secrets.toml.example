# 이 파일은 "형식 안내용 견본"입니다. 실제 키는 넣지 마세요.
#
# [로컬(내 컴퓨터)에서 테스트할 때]
#   이 파일을 복사해서 같은 폴더에 secrets.toml 이라는 이름으로 저장하고,
#   아래 값에 진짜 키를 넣으세요. secrets.toml 은 .gitignore 로 막혀 있어
#   GitHub 에는 올라가지 않습니다.
#
# [Streamlit Community Cloud 에 배포할 때]
#   이 파일을 올릴 필요 없이, 배포 화면의 Secrets 설정란에
#   아래 한 줄(진짜 키 포함)을 그대로 붙여넣으면 됩니다.

ANTHROPIC_API_KEY = "sk-ant-여기에-실제-키"
